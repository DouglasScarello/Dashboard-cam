# Python artifacts
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/
olho_de_deus/.venv/
*.egg-info/

# Node/JavaScript
node_modules/
**/node_modules/
dist/
**/dist/
.DS_Store

# Tauri / Rust build artifacts
**/src-tauri/target/
**/src-tauri/binaries/
target/
**/target/

# Local binaries/downloads
Miniconda3-latest-Linux-x86_64.sh
cv2
vidgear
yt_dlp

# Imagens (faces baixadas — não subir ao GitHub)
*.jpg
*.jpeg
*.png
!uploaded_media_*.png
# Ícones do app Tauri são build-essenciais, não "faces baixadas" — sem
# eles o `cargo build` quebra num clone novo (aconteceu, ver PLANO_CONTINUACAO.md).
!catalog/src-tauri/app-icon.png
!catalog/src-tauri/icons/**

# Banco de dados e embeddings (pesados — ficam local)
data/
intelligence/data/
olho_de_deus/data/
*.db
*.faiss
*.json.bak
live_cameras.json.bak-*
# Estado de liveness é gerado por camera_liveness.py, muda a cada rodada —
# dado de execução, não código-fonte.
database/camera_liveness_state.json

# Modelos YOLO (pesados — baixar via script)
*.pt
*.onnx

# Poetry lock (opcional manter, mas pesado)
# poetry.lock

# Logs de execução dos serviços systemd. Os .log antigos que já estão
# rastreados (audit.log, live.log, killswitch.log) continuam versionados de
# propósito — são trilha de auditoria, não ruído.
olho_de_deus/logs/api_server.log
olho_de_deus/logs/camera_curation.log
olho_de_deus/logs/camera_grid_server.log
olho_de_deus/logs/monitor_camera.log
olho_de_deus/logs/monitor_plates.log

# Scripts descartáveis de patch/exploração — one-offs de sessão, não código
# do sistema. Ficam no disco pra consulta, fora do histórico.
scratch/
patch_*.py
test_cors.js

# Outros
.pdm-python
.python-version
forense_*.jpg
.env
