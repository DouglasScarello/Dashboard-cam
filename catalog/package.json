{
    "name": "intelligence-catalog",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx @tauri-apps/cli dev",
        "build": "npx @tauri-apps/cli build"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2"
    }
}